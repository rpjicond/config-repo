server:
  port: 8084

spring:
  application:
    name: student-service

  datasource:
    url: jdbc:postgresql://student-db:5432/student_db
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak:8081/realms/edutrack
          jwk-set-uri: http://keycloak:8081/realms/edutrack/protocol/openid-connect/certs

  kafka:
    bootstrap-servers: edutrack-kafka:29092
    consumer:
      group-id: student-service
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "com.educ.student_service.messaging.event"
        spring.json.use.type.headers: false

    topics:
      user-created: identity.user.created

  data:
    redis:
      password: EDtrack@2025!
      cluster:
        nodes:
          - educ-plataforma-redis-node-1-1:6379
          - educ-plataforma-redis-node-2-1:6379
          - educ-plataforma-redis-node-3-1:6379
          - educ-plataforma-redis-node-4-1:6379
          - educ-plataforma-redis-node-5-1:6379
          - educ-plataforma-redis-node-6-1:6379
      timeout: 2000ms

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/
  instance:
    preferIpAddress: true
    lease-renewal-interval-in-seconds: 10
    lease-expiration-duration-in-seconds: 30

management:
  endpoints:
    web:
      exposure:
        include: "*"

  tracing:
    sampling:
      probability: 1.0

logging:
  level:
    com.educ.student_service: DEBUG
    org.springframework.kafka: INFO
    org.springframework.data.redis: INFO
